<%= form_with(model: pet) do |f| %>
  <%= render "shared/error_messages", object: f.object %>

  <div class="mb-3">
    <%= f.label :name, class: "form-label" %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :photo, class: "form-label" %>
    
    <% if pet.persisted? && pet.photo.attached? %>
      <div class="mb-2">
        <span class="d-block small text-muted mb-1">Current photo:</span>
        <%= image_tag pet.photo.variant(resize_to_limit: [100, 100]), class: "img-thumbnail" %>
      </div>
    <% end %>

    <%= f.file_field :photo, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :species, class: "form-label" %>
    <%= f.select :species, ["dog", "cat", "rabbit", "bird", "reptile", "other"], {}, class: "form-select" %>
  </div>

  <div class="mb-3">
    <%= f.label :breed, class: "form-label" %>
    <%= f.text_field :breed, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :weight, class: "form-label" %>
    <%= f.number_field :weight, step: :any, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :date_of_birth, class: "form-label" %>
    <%= f.date_field :date_of_birth, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :owner_id, "Owner", class: "form-label" %>
    <%= f.collection_select :owner_id, Owner.all, :id, :first_name, { prompt: "Select an owner" }, class: "form-select" %>
  </div>

  <%= f.submit class: "btn btn-primary w-100" %>
<% end %>
